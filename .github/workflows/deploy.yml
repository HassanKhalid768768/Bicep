name: Deploy Bicep Template to Azure

on:
  push:
    branches:
      - main  # or your preferred branch

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Login to Azure
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Deploy Bicep Template
      uses: azure/arm-deploy@v1
      with:
        scope: resourceGroup  # Change scope to resourceGroup
        subscriptionId: ${{ fromJson(secrets.AZURE_CREDENTIALS).subscriptionId }}
        resourceGroupName: '<your-resource-group-name>'  # Ensure this is set correctly
        region: eastus
        template: ./main.bicep
        parameters: |
          adminPassword=${{ secrets.ADMIN_PASSWORD }}  # Ensure this is set if needed
